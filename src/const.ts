export enum HTTP_FIELD {
    REQUEST_PARSER = "$$http_request_parser",
    SENDER = "$$http_sender",
    RESPONSE_REFINER = "$$http_response_refinder",
    CORS = "$$http_cors",
}

export enum HTTP_ROLE {
    SERVICE = "$$http_service",
}
